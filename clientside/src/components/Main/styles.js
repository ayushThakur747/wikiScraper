import {makeStyles} from '@material-ui/core';

export default makeStyles({
    container:{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        padding: "2em"
    },
    heading:{
        padding:"1em"
    },
    button:{
        alignItems: 'center',
        margin:"1em"
    },
    input:{
        alignItems: 'center',
    }
})